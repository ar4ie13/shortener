# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test client
cmd/client
cmd/grpstest

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.cov

# Binariesß
cmd/shortener/main
cmd/shortener/shortener
bin

# Dependency directories (remove the comment below to include it)
vendor/

# IDEs directories
.idea

# Makefile
Makefile

# json.l filestorage files
storage.jsonl
internal/repository/filestorage/test.jsonl
audit.jsonl
audit_test.jsonl
config.json

# linters configs
staticcheck.conf

# .gitignore
.gitignore
